# loadtestsample

Load test sample using Zerocode, Spring and JUnit.

To run test:

1. Download the project
2. Set desired config at src/main/resources/config.properties
3. Run /src/main/java/loadtest/GoogleLoadTest.java
4. Check results via opening /target/zerocode-junit-interactive-fuzzy-search.html in browser
